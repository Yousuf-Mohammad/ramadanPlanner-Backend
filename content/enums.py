from common.enums import BaseEnum


class ContentType(BaseEnum):
    """
    ContentType is an enum that represents the type of content.
    """

    QURAN = "quran"
    HADITH = "hadith"
    DUA = "dua"
    QUOTE = "quote"
